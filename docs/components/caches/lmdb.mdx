---
title: LMDB
---

You can use [LMDB](https://dbdb.io/db/lmdb) to cache values locally on disk.

## Install LMDB addon

```bash
npm install @llm-tools/embedjs-lmdb
```

## Usage
<CodeGroup>
```ts Partial example
import { RAGApplicationBuilder } from '@llm-tools/embedjs';
import { LmdbCache } from '@llm-tools/embedjs-lmdb';

const app = await new RAGApplicationBuilder()
.setCache(new LmdbCache({ path: path.resolve('./cache') }))
```
</CodeGroup>

<Snippet file="missing-cache-tip.mdx" />
